<!DOCTYPE html>
<html>

<head>
</head>

<body>
    <div class="mt-4"></div>
    <h2 style="color: red">Điều khiển các thiết bị</h2> {{#each devices}}

    <div class="mt-4">

        <div class="row">

            <div class="mt-ios">
                <p style="color: blue">{{this.Name}}</p>
                <a class="{{this.type}}" href="/control/{{this._id}}" role="button">{{this.Show}}</a>
            </div>
        </div>
        <div class="mt-4"></div>
    </div>
    {{/each}}